# SQLite for local demo (default)
DATABASE_URL="file:./dev.db"

# For company/private deploy, switch to PostgreSQL, e.g.:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/temp_fence_ops?schema=public"
